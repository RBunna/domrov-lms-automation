import { Test, TestingModule } from '@nestjs/testing';
import { beforeAll, beforeEach, describe, expect, it, jest } from '@jest/globals';
import { getRepositoryToken } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { JwtService } from '@nestjs/jwt';
import { MailerService } from '@nestjs-modules/mailer';
import {
    BadRequestException,
    ConflictException,
    NotFoundException,
    UnauthorizedException,
    InternalServerErrorException,
} from '@nestjs/common';

import { AuthService } from '../src/modules/auth/auth.service';
import { User } from '../src/libs/entities/user/user.entity';
import { UserEmailOtp } from '../src/libs/entities/user/user-email-otp.entity';
import { UserRefreshToken } from '../src/libs/entities/user/user-refresh-token.entity';
import { RegisterUserDTO } from '../src/libs/dtos/user/register-user.dto';
import { LoginUserDTO } from '../src/libs/dtos/user/login.dto';
import { UserStatus } from '../src/libs/enums/Status';
import { Encryption } from '../src/libs/utils/Encryption'; // Real util - NO mock
import { UserService } from '../src/modules/user/user.service';

describe('AuthService - Comprehensive Testing with Detailed Logic Verification', () => {
    let service: AuthService;
    let userService: jest.Mocked<UserService>;
    let userEmailOtpRepo: jest.Mocked<Repository<UserEmailOtp>>;
    let userRefreshTokenRepo: jest.Mocked<Repository<UserRefreshToken>>;
    let jwtService: jest.Mocked<JwtService>;
    let mailerService: jest.Mocked<MailerService>;

    let realHashedPassword: string;
    let anotherHashedPassword: string;

    const baseUser: Partial<User> = {
        id: 1,
        email: 'test@example.com',
        firstName: 'John',
        lastName: 'Doe',
        isVerified: false,
        status: UserStatus.ACTIVE,
    };

    const mockOtpRecord = () => ({
        id: 10,
        otp: '123456',
        expiresAt: new Date(Date.now() + 10 * 60 * 1000),
        user: { id: 1 },
    });

    beforeAll(async () => {
        // Pre-compute real hashed passwords for tests
        realHashedPassword = await Encryption.hashPassword('SecurePass123!');
        anotherHashedPassword = await Encryption.hashPassword('DifferentPass456!');
    });

    beforeEach(async () => {
        jest.clearAllMocks();

        const module: TestingModule = await Test.createTestingModule({
            providers: [
                AuthService,
                {
                    provide: UserService,
                    useValue: {
                        findByEmail: jest.fn(),
                        create: jest.fn(),
                        verifyUser: jest.fn(),
                        validateOAuthUser: jest.fn(),
                        updateUser: jest.fn(),
                        findById: jest.fn(),
                    },
                },
                {
                    provide: getRepositoryToken(UserEmailOtp),
                    useValue: {
                        findOne: jest.fn(),
                        save: jest.fn(),
                        create: jest.fn(),
                        delete: jest.fn(),
                        find: jest.fn(),
                    },
                },
                {
                    provide: getRepositoryToken(UserRefreshToken),
                    useValue: {
                        save: jest.fn(),
                        delete: jest.fn(),
                        findOne: jest.fn(),
                    },
                },
                { provide: JwtService, useValue: { signAsync: jest.fn(), verifyAsync: jest.fn() } },
                { provide: MailerService, useValue: { sendMail: jest.fn() } },
            ],
        }).compile();

        service = module.get<AuthService>(AuthService);
        userService = module.get(UserService) as jest.Mocked<UserService>;
        userEmailOtpRepo = module.get(getRepositoryToken(UserEmailOtp)) as jest.Mocked<Repository<UserEmailOtp>>;
        userRefreshTokenRepo = module.get(getRepositoryToken(UserRefreshToken)) as jest.Mocked<Repository<UserRefreshToken>>;
        jwtService = module.get(JwtService) as jest.Mocked<JwtService>;
        mailerService = module.get(MailerService) as jest.Mocked<MailerService>;
    });

    // ============================================================
    // SIGN UP - Detailed Logic & Validation Tests
    // ============================================================
    describe('signUp - Comprehensive Registration Logic', () => {
        const validRegisterDto: RegisterUserDTO = {
            email: 'newuser@example.com',
            password: 'SecurePass123!',
            confirmPassword: 'SecurePass123!',
            firstName: 'New',
            lastName: 'User',
        } as RegisterUserDTO;

        describe('Input Validation', () => {
            it('SIGNUP_VALIDATE_001 - should reject null DTO', async () => {
                await expect(service.signUp(null as any)).rejects.toThrow(
                    new BadRequestException('Registration data is required'),
                );
            });

            it('SIGNUP_VALIDATE_002 - should reject undefined DTO', async () => {
                await expect(service.signUp(undefined as any)).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('SIGNUP_VALIDATE_003 - should reject missing email', async () => {
                const invalidDto = { ...validRegisterDto, email: undefined };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    new BadRequestException(/email/i),
                );
            });

            it('SIGNUP_VALIDATE_004 - should reject empty email', async () => {
                const invalidDto = { ...validRegisterDto, email: '' };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('SIGNUP_VALIDATE_005 - should reject invalid email format', async () => {
                const invalidDto = { ...validRegisterDto, email: 'not-an-email' };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    new BadRequestException(/email.*format|invalid/i),
                );
            });

            it('SIGNUP_VALIDATE_006 - should reject missing password', async () => {
                const invalidDto = { ...validRegisterDto, password: undefined };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('SIGNUP_VALIDATE_007 - should reject short password', async () => {
                const invalidDto = { ...validRegisterDto, password: 'Short1!', confirmPassword: 'Short1!' };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    new BadRequestException(/password.*8|length/i),
                );
            });

            it('SIGNUP_VALIDATE_008 - should reject password without uppercase', async () => {
                const invalidDto = {
                    ...validRegisterDto,
                    password: 'securepass123!',
                    confirmPassword: 'securepass123!',
                };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    new BadRequestException(/uppercase|password/i),
                );
            });

            it('SIGNUP_VALIDATE_009 - should reject password without number', async () => {
                const invalidDto = {
                    ...validRegisterDto,
                    password: 'SecurePass!',
                    confirmPassword: 'SecurePass!',
                };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    new BadRequestException(/number|digit|password/i),
                );
            });

            it('SIGNUP_VALIDATE_010 - should reject password without special character', async () => {
                const invalidDto = {
                    ...validRegisterDto,
                    password: 'SecurePass123',
                    confirmPassword: 'SecurePass123',
                };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    new BadRequestException(/special|character|password/i),
                );
            });

            it('SIGNUP_VALIDATE_011 - should reject mismatched passwords', async () => {
                const invalidDto = {
                    ...validRegisterDto,
                    password: 'SecurePass123!',
                    confirmPassword: 'DifferentPass123!',
                };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    new BadRequestException(/match|confirm/i),
                );
            });

            it('SIGNUP_VALIDATE_012 - should reject missing firstName', async () => {
                const invalidDto = { ...validRegisterDto, firstName: undefined };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('SIGNUP_VALIDATE_013 - should reject missing lastName', async () => {
                const invalidDto = { ...validRegisterDto, lastName: undefined };
                await expect(service.signUp(invalidDto as any)).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('SIGNUP_VALIDATE_014 - should accept valid email formats', async () => {
                const validEmails = [
                    'user@example.com',
                    'user.name@example.com',
                    'user+tag@example.co.uk',
                    'user_123@example-domain.com',
                ];

                for (const email of validEmails) {
                    userService.findByEmail.mockResolvedValue(null);
                    userService.create.mockResolvedValue({
                        id: 1,
                        email,
                        firstName: 'Test',
                        lastName: 'User',
                    } as User);

                    const result = await service.signUp({
                        ...validRegisterDto,
                        email,
                    } as RegisterUserDTO);

                    expect(result.email).toBe(email);
                }
            });
        });

        describe('Email Conflict Detection', () => {
            it('SIGNUP_CONFLICT_001 - should throw ConflictException when email exists', async () => {
                userService.findByEmail.mockResolvedValue({ id: 999 } as User);

                await expect(service.signUp(validRegisterDto)).rejects.toThrow(
                    new ConflictException(/email.*already.*exists|email.*taken/i),
                );
            });

            it('SIGNUP_CONFLICT_002 - should verify email lookup before creation', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({ id: 1, ...validRegisterDto } as unknown as User);

                await service.signUp(validRegisterDto);

                expect(userService.findByEmail).toHaveBeenCalledWith(validRegisterDto.email);
                expect(userService.findByEmail).toHaveBeenCalledTimes(1);
            });

            it('SIGNUP_CONFLICT_003 - should handle case-insensitive email check', async () => {
                const upperCaseEmail = 'NEWUSER@EXAMPLE.COM';
                const lowerCaseEmail = 'newuser@example.com';

                userService.findByEmail.mockImplementation(async (email: string) => {
                    // Simulate case-insensitive lookup
                    if (email.toLowerCase() === lowerCaseEmail) {
                        return { id: 1 } as User;
                    }
                    return null;
                });

                await expect(
                    service.signUp({ ...validRegisterDto, email: upperCaseEmail } as RegisterUserDTO),
                ).rejects.toThrow(ConflictException);
            });
        });

        describe('Password Hashing & Security', () => {
            it('SIGNUP_SECURITY_001 - should hash password before storage', async () => {
                const plainPassword = 'SecurePass123!';
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({ id: 1, email: 'test@example.com' } as User);

                await service.signUp({
                    ...validRegisterDto,
                    password: plainPassword,
                } as RegisterUserDTO);

                // Verify create was called with hashed password
                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.password).not.toBe(plainPassword);
                expect(createCall.password.length).toBeGreaterThan(20); // Hash is longer

                // Verify hash can be verified
                const isValid = await Encryption.verifyPassword(plainPassword, createCall.password);
                expect(isValid).toBe(true);
            });

            it('SIGNUP_SECURITY_002 - should create different hash for same password', async () => {
                userService.findByEmail.mockResolvedValue(null);

                const password1 = 'SecurePass123!';
                const password2 = 'SecurePass123!';

                const hash1 = await Encryption.hashPassword(password1);
                const hash2 = await Encryption.hashPassword(password2);

                // Hashes should be different due to salt
                expect(hash1).not.toBe(hash2);

                // But both should verify with same password
                const verify1 = await Encryption.verifyPassword(password1, hash1);
                const verify2 = await Encryption.verifyPassword(password2, hash2);

                expect(verify1).toBe(true);
                expect(verify2).toBe(true);
            });

            it('SIGNUP_SECURITY_003 - should not store plain password', async () => {
                userService.findByEmail.mockResolvedValue(null);
                
                const createSpy=userService.create.mockImplementation(async (data: Partial<User>) => ({
                    id: 1,
                    ...data,
                } as User));

                await service.signUp(validRegisterDto);

                const createCall = createSpy.mock.calls[0][0];
                expect(createCall.password).not.toContain('SecurePass123!');
                expect(createCall.password).not.toContain(validRegisterDto.password);
            });

            it('SIGNUP_SECURITY_004 - should verify password hash length is adequate', async () => {
                const password = 'TestPassword123!';
                const hash = await Encryption.hashPassword(password);

                // Hash should be long enough to be secure (bcrypt produces ~60 char hash)
                expect(hash.length).toBeGreaterThanOrEqual(50);

                // Should have proper format ($2a$, $2b$, or $2y$ for bcrypt)
                expect(hash).toMatch(/^\$2[aby]\$/);
            });
        });

        describe('User Creation & Response', () => {
            it('SIGNUP_CREATE_001 - should create user with correct data', async () => {
                userService.findByEmail.mockResolvedValue(null);
                const createdUser = {
                    id: 123,
                    email: validRegisterDto.email,
                    firstName: validRegisterDto.firstName,
                    lastName: validRegisterDto.lastName,
                    isVerified: false,
                    status: UserStatus.ACTIVE,
                } as User;
                userService.create.mockResolvedValue(createdUser);

                const result = await service.signUp(validRegisterDto);

                // Verify user service was called with correct data
                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.email).toBe(validRegisterDto.email);
                expect(createCall.firstName).toBe(validRegisterDto.firstName);
                expect(createCall.lastName).toBe(validRegisterDto.lastName);
            });

            it('SIGNUP_CREATE_002 - should return user data without password', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({
                    id: 123,
                    email: validRegisterDto.email,
                    firstName: validRegisterDto.firstName,
                    lastName: validRegisterDto.lastName,
                } as User);

                const result = await service.signUp(validRegisterDto);

                expect(result.userId).toBe(123);
                expect(result.email).toBe(validRegisterDto.email);
                expect(result.firstName).toBe(validRegisterDto.firstName);
                expect(result.lastName).toBe(validRegisterDto.lastName);
                
            });

            it('SIGNUP_CREATE_003 - should initialize user as unverified', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({
                    id: 1,
                    email: validRegisterDto.email,
                    isVerified: false,
                } as User);

                await service.signUp(validRegisterDto);

                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.isVerified).toBe(false);
            });

            it('SIGNUP_CREATE_004 - should set user status to ACTIVE', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({
                    id: 1,
                    status: UserStatus.ACTIVE,
                } as User);

                await service.signUp(validRegisterDto);

                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.status).toBe(UserStatus.ACTIVE);
            });

            it('SIGNUP_CREATE_005 - should handle user creation failure', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockRejectedValue(new Error('Database error'));

                await expect(service.signUp(validRegisterDto)).rejects.toThrow();
            });
        });

        describe('Email Format & Normalization', () => {
            it('SIGNUP_EMAIL_001 - should normalize email to lowercase', async () => {
                const upperCaseEmail = 'TEST@EXAMPLE.COM';
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({ id: 1, email: upperCaseEmail } as User);

                await service.signUp({
                    ...validRegisterDto,
                    email: upperCaseEmail,
                } as RegisterUserDTO);

                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.email).toBe(upperCaseEmail.toLowerCase());
            });

            it('SIGNUP_EMAIL_002 - should trim email whitespace', async () => {
                const emailWithWhitespace = '  test@example.com  ';
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({ id: 1, email: 'test@example.com' } as User);

                await service.signUp({
                    ...validRegisterDto,
                    email: emailWithWhitespace,
                } as RegisterUserDTO);

                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.email).toBe('test@example.com');
            });
        });
    });

    // ============================================================
    // LOGIN - Detailed Logic & Security Verification
    // ============================================================
    describe('login - Comprehensive Authentication Logic', () => {
        const validLoginDto: LoginUserDTO = {
            email: 'test@example.com',
            password: 'SecurePass123!',
        } as LoginUserDTO;

        describe('Input Validation', () => {
            it('LOGIN_VALIDATE_001 - should reject null DTO', async () => {
                await expect(service.login(null as any)).rejects.toThrow(BadRequestException);
            });

            it('LOGIN_VALIDATE_002 - should reject missing email', async () => {
                await expect(
                    service.login({ password: 'SecurePass123!' } as LoginUserDTO),
                ).rejects.toThrow(BadRequestException);
            });

            it('LOGIN_VALIDATE_003 - should reject empty email', async () => {
                await expect(
                    service.login({ email: '', password: 'SecurePass123!' } as LoginUserDTO),
                ).rejects.toThrow(BadRequestException);
            });

            it('LOGIN_VALIDATE_004 - should reject missing password', async () => {
                await expect(
                    service.login({ email: 'test@example.com' } as LoginUserDTO),
                ).rejects.toThrow(BadRequestException);
            });

            it('LOGIN_VALIDATE_005 - should reject empty password', async () => {
                await expect(
                    service.login({ email: 'test@example.com', password: '' } as LoginUserDTO),
                ).rejects.toThrow(BadRequestException);
            });
        });

        describe('User Lookup & Authentication', () => {
            it('LOGIN_AUTH_001 - should throw UnauthorizedException for non-existent user', async () => {
                userService.findByEmail.mockResolvedValue(null);

                await expect(service.login(validLoginDto)).rejects.toThrow(
                    new UnauthorizedException(/invalid.*credentials|user.*not.*found/i),
                );
            });

            it('LOGIN_AUTH_002 - should verify user lookup by email', async () => {
                userService.findByEmail.mockResolvedValue(null);

                try {
                    await service.login(validLoginDto);
                } catch (e) {
                    // Expected to fail
                }

                expect(userService.findByEmail).toHaveBeenCalledWith(validLoginDto.email);
                expect(userService.findByEmail).toHaveBeenCalledTimes(1);
            });
        });

        describe('Password Verification', () => {
            it('LOGIN_PASSWORD_001 - should throw UnauthorizedException for wrong password', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    password: realHashedPassword,
                } as User);

                await expect(
                    service.login({ email: 'test@example.com', password: 'WrongPassword123!' } as LoginUserDTO),
                ).rejects.toThrow(UnauthorizedException);
            });

            it('LOGIN_PASSWORD_002 - should verify password using bcrypt comparison', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    password: realHashedPassword,
                } as User);

                // Verify password is correctly compared
                const isValid = await Encryption.verifyPassword('SecurePass123!', realHashedPassword);
                expect(isValid).toBe(true);
            });

            it('LOGIN_PASSWORD_003 - should reject wrong password even if close', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    password: realHashedPassword,
                } as User);

                const wrongPasswords = [
                    'SecurePass123', // Missing special char
                    'SecurePass124!', // Different number
                    'securepass123!', // Different case
                    'SecurePass123! ', // Extra space
                ];

                for (const wrongPass of wrongPasswords) {
                    await expect(
                        service.login({ email: 'test@example.com', password: wrongPass } as LoginUserDTO),
                    ).rejects.toThrow(UnauthorizedException);
                }
            });

            it('LOGIN_PASSWORD_004 - should successfully authenticate with correct password', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access-token').mockResolvedValueOnce('refresh-token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.login(validLoginDto);

                expect(result).toHaveProperty('accessToken');
                expect(result).toHaveProperty('refreshToken');
            });
        });

        describe('JWT Token Generation', () => {
            it('LOGIN_JWT_001 - should generate access and refresh tokens', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.login(validLoginDto);

                expect(jwtService.signAsync).toHaveBeenCalledTimes(2);
                expect(result.accessToken).toBe('access.token');
                expect(result.refreshToken).toBe('refresh.token');
            });

            it('LOGIN_JWT_002 - should include user ID in token payload', async () => {
                const userId = 123;
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: userId,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.login(validLoginDto);

                const firstCall = jwtService.signAsync.mock.calls[0];
                expect(firstCall[0]).toHaveProperty('sub', userId);
            });

            it('LOGIN_JWT_003 - should include email in token payload', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.login(validLoginDto);

                const firstCall = jwtService.signAsync.mock.calls[0];
                expect(firstCall[0]).toHaveProperty('email', validLoginDto.email);
            });

            it('LOGIN_JWT_004 - should set appropriate expiration times', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.login(validLoginDto);

                // Access token (shorter expiry)
                const accessCall = jwtService.signAsync.mock.calls[0];
                expect(accessCall[1]).toHaveProperty('expiresIn', '15m');

                // Refresh token (longer expiry)
                const refreshCall = jwtService.signAsync.mock.calls[1];
                expect(refreshCall[1]).toHaveProperty('expiresIn', '7d');
            });
        });

        describe('Refresh Token Storage', () => {
            it('LOGIN_REFRESH_001 - should save refresh token to database', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.login(validLoginDto);

                expect(userRefreshTokenRepo.save).toHaveBeenCalledTimes(1);
            });

            it('LOGIN_REFRESH_002 - should store refresh token with user association', async () => {
                const userId = 123;
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: userId,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.login(validLoginDto);

                const saveCall = userRefreshTokenRepo.save.mock.calls[0][0];
                expect(saveCall.user.id).toBe(userId);
                expect(saveCall.refreshToken).toBe('refresh.token');
            });

            it('LOGIN_REFRESH_003 - should store token expiration', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.login(validLoginDto);

                const saveCall = userRefreshTokenRepo.save.mock.calls[0][0];
                expect(saveCall).toHaveProperty('expiresAt');
                expect(saveCall.expiresAt instanceof Date).toBe(true);
                expect((saveCall.expiresAt as Date).getTime()).toBeGreaterThan(Date.now());
            });
        });

        describe('Response Format', () => {
            it('LOGIN_RESPONSE_001 - should return correct response structure', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.login(validLoginDto);

                expect(result).toEqual({
                    accessToken: 'access.token',
                    refreshToken: 'refresh.token',
                });
                expect(Object.keys(result).length).toBe(2);
            });

            it('LOGIN_RESPONSE_002 - should not return password in response', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.login(validLoginDto);

                expect(result).not.toHaveProperty('password');
                expect(JSON.stringify(result)).not.toContain('SecurePass123!');
            });
        });
    });

    // ============================================================
    // REFRESH TOKEN - Token Rotation Logic
    // ============================================================
    describe('refreshToken - Token Rotation & Verification', () => {
        describe('Input Validation', () => {
            it('REFRESH_VALIDATE_001 - should reject empty user ID', async () => {
                await expect(service.refreshToken('', 'test@example.com')).rejects.toThrow(
                    new BadRequestException(/user.*id|id.*required/i),
                );
            });

            it('REFRESH_VALIDATE_002 - should reject null user ID', async () => {
                await expect(service.refreshToken(null as any, 'test@example.com')).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('REFRESH_VALIDATE_003 - should reject empty email', async () => {
                await expect(service.refreshToken('123', '')).rejects.toThrow(BadRequestException);
            });

            it('REFRESH_VALIDATE_004 - should reject null email', async () => {
                await expect(service.refreshToken('123', null as any)).rejects.toThrow(BadRequestException);
            });
        });

        describe('Token Generation', () => {
            it('REFRESH_TOKEN_001 - should generate new access token', async () => {
                jwtService.signAsync.mockResolvedValue('new-access-token');

                const result = await service.refreshToken('123', 'test@example.com');

                expect(result).toHaveProperty('accessToken', 'new-access-token');
            });

            it('REFRESH_TOKEN_002 - should include correct payload in new token', async () => {
                jwtService.signAsync.mockResolvedValue('new-access-token');

                await service.refreshToken('123', 'test@example.com');

                const signCall = jwtService.signAsync.mock.calls[0];
                expect(signCall[0]).toEqual({
                    sub: '123',
                    email: 'test@example.com',
                });
            });

            it('REFRESH_TOKEN_003 - should set correct expiration for new token', async () => {
                jwtService.signAsync.mockResolvedValue('new-access-token');

                await service.refreshToken('123', 'test@example.com');

                const signCall = jwtService.signAsync.mock.calls[0];
                expect(signCall[1]).toHaveProperty('expiresIn', '15m');
            });
        });

        describe('Response Format & Metadata', () => {
            it('REFRESH_RESPONSE_001 - should return success status', async () => {
                jwtService.signAsync.mockResolvedValue('new-access-token');

                const result = await service.refreshToken('123', 'test@example.com');

                expect(result).toHaveProperty('status', 'success');
            });

            it('REFRESH_RESPONSE_002 - should include issuedAt timestamp', async () => {
                jwtService.signAsync.mockResolvedValue('new-access-token');
                const beforeCall = Date.now();

                const result = await service.refreshToken('123', 'test@example.com');
                const afterCall = Date.now();

                expect(result).toHaveProperty('issuedAt');
                expect(typeof result.issuedAt).toBe('number');
                expect(result.issuedAt).toBeGreaterThanOrEqual(beforeCall);
                expect(result.issuedAt).toBeLessThanOrEqual(afterCall);
            });

            it('REFRESH_RESPONSE_003 - should return complete response structure', async () => {
                jwtService.signAsync.mockResolvedValue('new-access-token');

                const result = await service.refreshToken('123', 'test@example.com');

                expect(Object.keys(result).sort()).toEqual(['accessToken', 'issuedAt', 'status'].sort());
            });

            it('REFRESH_RESPONSE_004 - should not include refresh token in response', async () => {
                jwtService.signAsync.mockResolvedValue('new-access-token');

                const result = await service.refreshToken('123', 'test@example.com');

                expect(result).not.toHaveProperty('refreshToken');
            });
        });
    });

    // ============================================================
    // LOGOUT - Session Termination Logic
    // ============================================================
    describe('logout - Session Termination & Verification', () => {
        describe('Input Validation', () => {
            it('LOGOUT_VALIDATE_001 - should reject null user ID', async () => {
                await expect(service.logout(null as any, 'token')).rejects.toThrow(BadRequestException);
            });

            it('LOGOUT_VALIDATE_002 - should reject zero user ID', async () => {
                await expect(service.logout(0, 'token')).rejects.toThrow(BadRequestException);
            });

            it('LOGOUT_VALIDATE_003 - should reject negative user ID', async () => {
                await expect(service.logout(-1, 'token')).rejects.toThrow(BadRequestException);
            });

            it('LOGOUT_VALIDATE_004 - should reject null token', async () => {
                await expect(service.logout(1, null as any)).rejects.toThrow(BadRequestException);
            });

            it('LOGOUT_VALIDATE_005 - should reject empty token', async () => {
                await expect(service.logout(1, '')).rejects.toThrow(BadRequestException);
            });
        });

        describe('Token Deletion', () => {
            it('LOGOUT_DELETE_001 - should delete refresh token from database', async () => {
                userRefreshTokenRepo.delete.mockResolvedValue({ affected: 1 } as any);

                const result = await service.logout(1, 'token123');

                expect(userRefreshTokenRepo.delete).toHaveBeenCalledTimes(1);
                expect(result).toEqual({ message: 'Logged out successfully' });
            });

            it('LOGOUT_DELETE_002 - should query by user ID and token', async () => {
                userRefreshTokenRepo.delete.mockResolvedValue({ affected: 1 } as any);

                await service.logout(1, 'token123');

                const deleteCall = userRefreshTokenRepo.delete.mock.calls[0][0];
                expect(deleteCall).toEqual({
                    user: { id: 1 },
                    token: 'token123',
                });
            });

            it('LOGOUT_DELETE_003 - should throw NotFoundException if token not found', async () => {
                userRefreshTokenRepo.delete.mockResolvedValue({ affected: 0 } as any);

                await expect(service.logout(1, 'invalid-token')).rejects.toThrow(
                    new NotFoundException(/token.*not.*found|session.*not.*found/i),
                );
            });

            it('LOGOUT_DELETE_004 - should verify affected rows returned', async () => {
                userRefreshTokenRepo.delete.mockResolvedValue({ affected: 1 } as any);

                const result = await service.logout(1, 'token123');

                expect(userRefreshTokenRepo.delete).toHaveBeenCalled();
                expect(result.message).toContain('successfully');
            });
        });

        describe('Error Handling', () => {
            it('LOGOUT_ERROR_001 - should handle database error gracefully', async () => {
                userRefreshTokenRepo.delete.mockRejectedValue(new Error('Database error'));

                await expect(service.logout(1, 'token123')).rejects.toThrow();
            });

            it('LOGOUT_ERROR_002 - should differentiate between not found and error', async () => {
                userRefreshTokenRepo.delete.mockResolvedValue({ affected: 0 } as any);

                await expect(service.logout(1, 'invalid-token')).rejects.toThrow(NotFoundException);
            });
        });
    });

    // ============================================================
    // EMAIL VERIFICATION - OTP Logic & Timing
    // ============================================================
    describe('sendVerificationEmail - OTP Generation & Sending', () => {
        describe('Input Validation', () => {
            it('EMAIL_VALIDATE_001 - should reject null email', async () => {
                await expect(service.sendVerificationEmail(null as any)).rejects.toThrow(BadRequestException);
            });

            it('EMAIL_VALIDATE_002 - should reject empty email', async () => {
                await expect(service.sendVerificationEmail('')).rejects.toThrow(BadRequestException);
            });

            it('EMAIL_VALIDATE_003 - should reject invalid email format', async () => {
                await expect(service.sendVerificationEmail('not-an-email')).rejects.toThrow(BadRequestException);
            });
        });

        describe('User Verification Status', () => {
            it('EMAIL_STATUS_001 - should reject already verified users', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    isVerified: true,
                } as User);

                await expect(service.sendVerificationEmail('test@example.com')).rejects.toThrow(
                    new BadRequestException(/already.*verified|email.*verified/i),
                );
            });

            it('EMAIL_STATUS_002 - should accept unverified users', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);
                userEmailOtpRepo.findOne.mockResolvedValue(null);
                userEmailOtpRepo.save.mockResolvedValue({} as any);
                mailerService.sendMail.mockResolvedValue(undefined as any);

                const result = await service.sendVerificationEmail('test@example.com');

                expect(result.message).toContain('OTP sent');
            });

            it('EMAIL_STATUS_003 - should throw NotFoundException for non-existent user', async () => {
                userService.findByEmail.mockResolvedValue(null);

                await expect(service.sendVerificationEmail('nonexistent@example.com')).rejects.toThrow(
                    NotFoundException,
                );
            });
        });

        describe('OTP Generation & Timing', () => {
            it('EMAIL_OTP_001 - should generate 6-digit numeric OTP', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);
                userEmailOtpRepo.findOne.mockResolvedValue(null);
                userEmailOtpRepo.save.mockResolvedValue({} as any);
                mailerService.sendMail.mockResolvedValue(undefined as any);

                await service.sendVerificationEmail('test@example.com');

                const saveCall = userEmailOtpRepo.save.mock.calls[0][0];
                const otp = saveCall.otp;

                expect(otp).toMatch(/^\d{6}$/);
                expect(parseInt(otp)).toBeGreaterThanOrEqual(0);
                expect(parseInt(otp)).toBeLessThan(1000000);
            });

            it('EMAIL_OTP_002 - should set OTP expiration to 10 minutes', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);
                userEmailOtpRepo.findOne.mockResolvedValue(null);
                userEmailOtpRepo.save.mockResolvedValue({} as any);
                mailerService.sendMail.mockResolvedValue(undefined as any);

                const beforeCall = Date.now();
                await service.sendVerificationEmail('test@example.com');
                const afterCall = Date.now();

                const saveCall = userEmailOtpRepo.save.mock.calls[0][0];
                const expirationTime = (saveCall.expiresAt as Date).getTime();
                const expectedExpiration = beforeCall + 10 * 60 * 1000;

                expect(expirationTime).toBeGreaterThanOrEqual(expectedExpiration);
                expect(expirationTime).toBeLessThanOrEqual(afterCall + 10 * 60 * 1000);
            });

            it('EMAIL_OTP_003 - should replace existing OTP for same user', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);

                // User already has an OTP
                userEmailOtpRepo.findOne.mockResolvedValueOnce(mockOtpRecord() as any);
                userEmailOtpRepo.delete.mockResolvedValue({ affected: 1 } as any);
                userEmailOtpRepo.save.mockResolvedValue({} as any);
                mailerService.sendMail.mockResolvedValue(undefined as any);

                await service.sendVerificationEmail('test@example.com');

                expect(userEmailOtpRepo.delete).toHaveBeenCalled();
                expect(userEmailOtpRepo.save).toHaveBeenCalled();
            });

            it('EMAIL_OTP_004 - should create new OTP if none exists', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);
                userEmailOtpRepo.findOne.mockResolvedValue(null);
                userEmailOtpRepo.save.mockResolvedValue({} as any);
                mailerService.sendMail.mockResolvedValue(undefined as any);

                await service.sendVerificationEmail('test@example.com');

                expect(userEmailOtpRepo.save).toHaveBeenCalledTimes(1);
                const saveCall = userEmailOtpRepo.save.mock.calls[0][0];
                expect(saveCall.user.id).toBe(1);
            });
        });

        describe('Email Sending', () => {
            it('EMAIL_SEND_001 - should send email with OTP', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);
                userEmailOtpRepo.findOne.mockResolvedValue(null);
                userEmailOtpRepo.save.mockResolvedValue({} as any);
                mailerService.sendMail.mockResolvedValue(undefined as any);

                await service.sendVerificationEmail('test@example.com');

                expect(mailerService.sendMail).toHaveBeenCalled();
            });

            it('EMAIL_SEND_002 - should include OTP in email content', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);
                userEmailOtpRepo.findOne.mockResolvedValue(null);
                userEmailOtpRepo.save.mockResolvedValue({ otp: '123456' } as any);
                mailerService.sendMail.mockResolvedValue(undefined as any);

                await service.sendVerificationEmail('test@example.com');

                const sendCall = mailerService.sendMail.mock.calls[0][0];
                expect(sendCall.to).toBe('test@example.com');
                expect(sendCall.subject).toContain('verification');
            });

            it('EMAIL_SEND_003 - should handle email sending failure', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);
                userEmailOtpRepo.findOne.mockResolvedValue(null);
                userEmailOtpRepo.save.mockResolvedValue({} as any);
                mailerService.sendMail.mockRejectedValue(new Error('Email service error'));

                await expect(service.sendVerificationEmail('test@example.com')).rejects.toThrow();
            });
        });
    });

    // ============================================================
    // EMAIL OTP VERIFICATION - OTP Validation Logic
    // ============================================================
    describe('verifyEmailOtp - OTP Validation & Verification', () => {
        describe('Input Validation', () => {
            it('VERIFY_VALIDATE_001 - should reject null email', async () => {
                await expect(service.verifyEmailOtp(null as any, '123456')).rejects.toThrow(BadRequestException);
            });

            it('VERIFY_VALIDATE_002 - should reject empty email', async () => {
                await expect(service.verifyEmailOtp('', '123456')).rejects.toThrow(BadRequestException);
            });

            it('VERIFY_VALIDATE_003 - should reject null OTP', async () => {
                await expect(service.verifyEmailOtp('test@example.com', null as any)).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('VERIFY_VALIDATE_004 - should reject empty OTP', async () => {
                await expect(service.verifyEmailOtp('test@example.com', '')).rejects.toThrow(BadRequestException);
            });

            it('VERIFY_VALIDATE_005 - should reject non-numeric OTP', async () => {
                await expect(service.verifyEmailOtp('test@example.com', 'abcdef')).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('VERIFY_VALIDATE_006 - should reject OTP with wrong length', async () => {
                await expect(service.verifyEmailOtp('test@example.com', '12345')).rejects.toThrow(
                    new BadRequestException(/otp.*6|length/i),
                );
            });
        });

        describe('User Lookup', () => {
            it('VERIFY_USER_001 - should throw NotFoundException for non-existent user', async () => {
                userService.findByEmail.mockResolvedValue(null);

                await expect(service.verifyEmailOtp('nonexistent@example.com', '123456')).rejects.toThrow(
                    NotFoundException,
                );
            });

            it('VERIFY_USER_002 - should reject already verified users', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    isVerified: true,
                } as User);

                await expect(service.verifyEmailOtp('test@example.com', '123456')).rejects.toThrow(
                    new BadRequestException(/already.*verified/i),
                );
            });
        });

        describe('OTP Validation', () => {
            it('VERIFY_OTP_001 - should throw NotFoundException if OTP not found', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);
                userEmailOtpRepo.findOne.mockResolvedValue(null);

                await expect(service.verifyEmailOtp('test@example.com', '123456')).rejects.toThrow(
                    new NotFoundException(/otp.*not.*found|invalid.*otp/i),
                );
            });

            it('VERIFY_OTP_002 - should reject expired OTP', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);

                const expiredOtp = {
                    id: 10,
                    otp: '123456',
                    expiresAt: new Date(Date.now() - 1000), // Expired 1 second ago
                    user: { id: 1 },
                };

                userEmailOtpRepo.findOne.mockResolvedValue(expiredOtp as any);

                await expect(service.verifyEmailOtp('test@example.com', '123456')).rejects.toThrow(
                    new BadRequestException(/expired|otp.*expired/i),
                );
            });

            it('VERIFY_OTP_003 - should reject incorrect OTP value', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);

                userEmailOtpRepo.findOne.mockResolvedValue(mockOtpRecord() as any);

                await expect(service.verifyEmailOtp('test@example.com', '654321')).rejects.toThrow(
                    new BadRequestException(/incorrect|invalid.*otp|does.*not.*match/i),
                );
            });

            it('VERIFY_OTP_004 - should accept valid non-expired OTP', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);

                const validOtp = {
                    id: 10,
                    otp: '123456',
                    expiresAt: new Date(Date.now() + 5 * 60 * 1000), // Expires in 5 minutes
                    user: { id: 1 },
                };

                userEmailOtpRepo.findOne.mockResolvedValue(validOtp as any);
                userEmailOtpRepo.delete.mockResolvedValue({ affected: 1 } as any);
                userService.verifyUser.mockResolvedValue({} as any);

                const result = await service.verifyEmailOtp('test@example.com', '123456');

                expect(result.message).toContain('verified');
            });

            it('VERIFY_OTP_005 - should verify exact OTP match', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);

                const validOtp = {
                    id: 10,
                    otp: '123456',
                    expiresAt: new Date(Date.now() + 5 * 60 * 1000),
                    user: { id: 1 },
                };

                userEmailOtpRepo.findOne.mockResolvedValue(validOtp as any);
                userEmailOtpRepo.delete.mockResolvedValue({} as any);
                userService.verifyUser.mockResolvedValue({} as any);

                // Test similar OTPs
                const wrongOtps = ['123455', '123457', '012345'];

                for (const wrongOtp of wrongOtps) {
                    userEmailOtpRepo.findOne.mockResolvedValue(validOtp as any);
                    await expect(service.verifyEmailOtp('test@example.com', wrongOtp)).rejects.toThrow(
                        BadRequestException,
                    );
                }
            });
        });

        describe('User Verification & Cleanup', () => {
            it('VERIFY_COMPLETE_001 - should update user as verified', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);

                userEmailOtpRepo.findOne.mockResolvedValue(mockOtpRecord() as any);
                userEmailOtpRepo.delete.mockResolvedValue({} as any);
                userService.verifyUser.mockResolvedValue({} as any);

                await service.verifyEmailOtp('test@example.com', '123456');

                expect(userService.verifyUser).toHaveBeenCalledWith(1);
            });

            it('VERIFY_COMPLETE_002 - should delete OTP after verification', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);

                userEmailOtpRepo.findOne.mockResolvedValue(mockOtpRecord() as any);
                userEmailOtpRepo.delete.mockResolvedValue({ affected: 1 } as any);
                userService.verifyUser.mockResolvedValue({} as any);

                await service.verifyEmailOtp('test@example.com', '123456');

                expect(userEmailOtpRepo.delete).toHaveBeenCalled();
            });

            it('VERIFY_COMPLETE_003 - should return success message', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    isVerified: false,
                } as User);

                userEmailOtpRepo.findOne.mockResolvedValue(mockOtpRecord() as any);
                userEmailOtpRepo.delete.mockResolvedValue({} as any);
                userService.verifyUser.mockResolvedValue({} as any);

                const result = await service.verifyEmailOtp('test@example.com', '123456');

                expect(result.message).toContain('verified');
            });
        });
    });

    // ============================================================
    // OAUTH LOGIN - User Creation & Token Generation
    // ============================================================
    describe('oauthLogin - OAuth User Management & Token Generation', () => {
        describe('Input Validation', () => {
            it('OAUTH_VALIDATE_001 - should reject null profile', async () => {
                await expect(service.oauthLogin(null as any)).rejects.toThrow(BadRequestException);
            });

            it('OAUTH_VALIDATE_002 - should reject missing email', async () => {
                await expect(service.oauthLogin({ firstName: 'Test', lastName: 'User' } as any)).rejects.toThrow(
                    BadRequestException,
                );
            });

            it('OAUTH_VALIDATE_003 - should accept minimal valid profile', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({
                    id: 1,
                    email: 'oauth@example.com',
                    isVerified: true,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.oauthLogin({ email: 'oauth@example.com' });

                expect(result).toHaveProperty('accessToken');
                expect(result).toHaveProperty('refreshToken');
            });
        });

        describe('New User Creation', () => {
            it('OAUTH_NEW_001 - should create new OAuth user if not exists', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({
                    id: 999,
                    email: 'oauth@example.com',
                    firstName: 'OAuth',
                    lastName: 'User',
                    isVerified: true,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const profile = {
                    email: 'oauth@example.com',
                    firstName: 'OAuth',
                    lastName: 'User',
                };

                await service.oauthLogin(profile);

                expect(userService.create).toHaveBeenCalledWith(
                    expect.objectContaining({
                        email: profile.email,
                        firstName: profile.firstName,
                        lastName: profile.lastName,
                    }),
                );
            });

            it('OAUTH_NEW_002 - should set OAuth user as verified', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({
                    id: 999,
                    email: 'oauth@example.com',
                    isVerified: true,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.oauthLogin({ email: 'oauth@example.com' });

                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.isVerified).toBe(true);
            });

            it('OAUTH_NEW_003 - should set OAuth user status to ACTIVE', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({
                    id: 999,
                    status: UserStatus.ACTIVE,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.oauthLogin({ email: 'oauth@example.com' });

                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.status).toBe(UserStatus.ACTIVE);
            });

            it('OAUTH_NEW_004 - should generate random password for OAuth users', async () => {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({ id: 999 } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.oauthLogin({ email: 'oauth@example.com' });

                const createCall = userService.create.mock.calls[0][0];
                expect(createCall.password).toBeDefined();
                expect(createCall.password.length).toBeGreaterThan(20); // Hash length
            });
        });

        describe('Existing User Handling', () => {
            it('OAUTH_EXISTING_001 - should use existing user if found', async () => {
                const existingUser = {
                    ...baseUser,
                    id: 1,
                    email: 'oauth@example.com',
                } as User;

                userService.findByEmail.mockResolvedValue(existingUser);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.oauthLogin({ email: 'oauth@example.com' });

                expect(userService.create).not.toHaveBeenCalled();
                expect(result).toHaveProperty('accessToken');
                expect(result).toHaveProperty('refreshToken');
            });

            it('OAUTH_EXISTING_002 - should not update existing user data', async () => {
                const existingUser = {
                    ...baseUser,
                    id: 1,
                    firstName: 'Original',
                } as User;

                userService.findByEmail.mockResolvedValue(existingUser);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.oauthLogin({ email: 'oauth@example.com', firstName: 'Updated' });

                // OAuth login doesn't call update on existing users - it just generates tokens
                expect(userService.findByEmail).toHaveBeenCalledWith('oauth@example.com');
            });

            it('OAUTH_EXISTING_003 - should generate tokens for existing user', async () => {
                const existingUser = { ...baseUser, id: 123 } as User;

                userService.findByEmail.mockResolvedValue(existingUser);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.oauthLogin({ email: 'oauth@example.com' });

                expect(jwtService.signAsync).toHaveBeenCalledTimes(2);
                expect(result.accessToken).toBe('access.token');
                expect(result.refreshToken).toBe('refresh.token');
            });
        });

        describe('Token Generation for OAuth', () => {
            it('OAUTH_TOKEN_001 - should include user ID in token', async () => {
                userService.findByEmail.mockResolvedValue({ ...baseUser, id: 999 } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.oauthLogin({ email: 'oauth@example.com' });

                const firstCall = jwtService.signAsync.mock.calls[0];
                expect(firstCall[0]).toHaveProperty('sub', 999);
            });

            it('OAUTH_TOKEN_002 - should include email in token', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    email: 'oauth@example.com',
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.oauthLogin({ email: 'oauth@example.com' });

                const firstCall = jwtService.signAsync.mock.calls[0];
                expect(firstCall[0]).toHaveProperty('email', 'oauth@example.com');
            });

            it('OAUTH_TOKEN_003 - should set appropriate token expiration times', async () => {
                userService.findByEmail.mockResolvedValue({ ...baseUser, id: 1 } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                await service.oauthLogin({ email: 'oauth@example.com' });

                // JwtService.signAsync is called without explicit expiration options in oauthLogin
                // The expiration is configured in the module setup (access-jwt.config.ts, refresh-jwt.config.ts)
                expect(jwtService.signAsync).toHaveBeenCalledTimes(2);
                expect(jwtService.signAsync).toHaveBeenCalledWith({ sub: 1, email: 'test@example.com' });
            });
        });

        describe('Response & Security', () => {
            it('OAUTH_RESPONSE_001 - should return access and refresh tokens', async () => {
                userService.findByEmail.mockResolvedValue({ ...baseUser, id: 1 } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.oauthLogin({ email: 'oauth@example.com' });

                expect(result).toEqual({
                    accessToken: 'access.token',
                    refreshToken: 'refresh.token',
                });
            });

            it('OAUTH_RESPONSE_002 - should not return user password', async () => {
                userService.findByEmail.mockResolvedValue({
                    ...baseUser,
                    id: 1,
                    password: realHashedPassword,
                } as User);
                jwtService.signAsync.mockResolvedValueOnce('access.token').mockResolvedValueOnce('refresh.token');
                userRefreshTokenRepo.save.mockResolvedValue({} as any);

                const result = await service.oauthLogin({ email: 'oauth@example.com' });

                expect(result).not.toHaveProperty('password');
                expect(JSON.stringify(result)).not.toContain('SecurePass');
            });
        });
    });

    // ============================================================
    // Edge Cases & Complex Scenarios
    // ============================================================
    describe('Edge Cases & Complex Scenarios', () => {
        it('EDGE_001 - should handle rapid token refresh requests', async () => {
            jwtService.signAsync.mockResolvedValue('new-access-token');

            const refreshPromises = Array(5)
                .fill(null)
                .map(() => service.refreshToken('123', 'test@example.com'));

            const results = await Promise.all(refreshPromises);

            expect(results.length).toBe(5);
            expect(jwtService.signAsync).toHaveBeenCalledTimes(5);
            results.forEach((result) => {
                expect(result).toHaveProperty('accessToken');
                expect(result).toHaveProperty('status', 'success');
            });
        });

        it('EDGE_002 - should handle simultaneous login attempts', async () => {
            userService.findByEmail.mockResolvedValue({
                ...baseUser,
                id: 1,
                password: realHashedPassword,
            } as User);
            jwtService.signAsync.mockResolvedValueOnce('access1').mockResolvedValueOnce('refresh1').mockResolvedValueOnce('access2').mockResolvedValueOnce('refresh2');
            userRefreshTokenRepo.save.mockResolvedValue({} as any);

            const loginPromises = Array(2)
                .fill(null)
                .map(() =>
                    service.login({
                        email: 'test@example.com',
                        password: 'SecurePass123!',
                    } as LoginUserDTO),
                );

            const results = await Promise.all(loginPromises);

            expect(results.length).toBe(2);
            expect(userRefreshTokenRepo.save).toHaveBeenCalledTimes(2);
        });

        it('EDGE_003 - should handle special characters in email', async () => {
            const specialEmails = [
                'user+test@example.com',
                'user.name@example.com',
                'user_123@example.com',
                'user-name@example.co.uk',
            ];

            for (const email of specialEmails) {
                userService.findByEmail.mockResolvedValue(null);
                userService.create.mockResolvedValue({
                    id: 1,
                    email,
                } as User);

                const result = await service.signUp({
                    email,
                    password: 'SecurePass123!',
                    confirmPassword: 'SecurePass123!',
                    firstName: 'Test',
                    lastName: 'User',
                } as RegisterUserDTO);

                expect(result.email).toBe(email);
            }
        });

        it('EDGE_004 - should handle very long passwords', async () => {
            const longPassword = 'A' + 'b'.repeat(98) + '1!'; // 101 char password
            userService.findByEmail.mockResolvedValue(null);
            userService.create.mockResolvedValue({
                id: 1,
                email: 'test@example.com',
            } as User);

            const result = await service.signUp({
                email: 'test@example.com',
                password: longPassword,
                confirmPassword: longPassword,
                firstName: 'Test',
                lastName: 'User',
            } as RegisterUserDTO);

            expect(result).toHaveProperty('userId');
        });

        it('EDGE_005 - should handle concurrent OTP verification attempts', async () => {
            userService.findByEmail.mockResolvedValue({
                ...baseUser,
                id: 1,
                isVerified: false,
            } as User);

            userEmailOtpRepo.findOne
                .mockResolvedValueOnce(mockOtpRecord() as any)
                .mockResolvedValueOnce(null);
            userEmailOtpRepo.delete.mockResolvedValue({ affected: 1 } as any);
            userService.verifyUser.mockResolvedValue({} as any);

            // First attempt succeeds
            const result1 = await service.verifyEmailOtp('test@example.com', '123456');
            expect(result1).toHaveProperty('message');

            // Second attempt fails (OTP already deleted)
            await expect(service.verifyEmailOtp('test@example.com', '123456')).rejects.toThrow(BadRequestException);
        });
    });
});