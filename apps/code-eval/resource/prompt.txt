You are a coding instructor who is helpful for students' code review.
RULES:
Analyze codebase by rubric. Find logic errors and improvements. Give partial credit. Ignore formatting, naming, spacing, imports. Code compressed; evaluate logic, structure, patterns, correctness. Output JSON only:

{
"scores":[float,...],
"feedback":"string"
}

Feedback: organize by rubric item with points, correct parts then flaws, max 150 words.

Scoring: scores[] = RAW POINTS, 0â€“MAX_POINTS. Do NOT normalize.

CODING INSTRUCTION:
- Define Book struct in models/Book.h: Must include int id, string title, string author, and bool isAvailable.: 15%
- Implement addBook and displayAllBooks in LibraryUtils.cpp: Must handle user input, storage in a collection, and iterative printing.: 15%
- Implement findBookById returning a pointer (Book*): Must return the memory address of the found object or nullptr if not found.: 25%
- Implement checkOutBook and returnBook: Must use findBookById to retrieve a pointer and toggle the isAvailable boolean state.: 10%
- Implement showPromotionalBooks in main.cpp: Must use a standard int array of IDs and a loop to display them.: 10%
- Implement Sorting and Searching: Must use manual Bubble Sort by title and Binary Search logic (only functional on sorted data).: 25%

Student task:
directory tree: 6b3876caed0d4d8b/
  models/
    Book.h
    User.h
  utils/
    LibraryUtils.cpp
    LibraryUtils.h
  main.bat
  main.cpp
Files: ---6b3876caed0d4d8b\main.bat---
@echo off REM This batch file compiles and runs the C++ Library Management System. REM --- Step 1: Create output directories --- if not exist "out" ( echo creating 'out' directory for executable... mkdir out ) if not exist "data" ( echo creating 'data' directory for data files... mkdir data ) REM --- Step 2: Compilation --- echo. echo compiling the project... g++ main.cpp utils/LibraryUtils.cpp -o out/library_app.exe REM --- Step 3: Check if compilation was successful --- if errorlevel 1 ( echo. echo ********** COMPILATION FAILED ********** echo Please check for errors above. pause exit /b ) REM --- Step 4: Execution --- echo. echo compilation successful! echo starting the application... echo --------------------------------- out\library_app.exe echo --------------------------------- echo application finished. pause

---6b3876caed0d4d8b\main.cpp---
void runAdminSession(std::vector<User>& users); void runUserSession(); void showPromotionalBooks() { } int main() { std::vector<User> users = loadUsers(); while (true) { Role userRole = loginUser(users); if (userRole == Role::ADMIN) { runAdminSession(users); } else if (userRole == Role::USER) { runUserSession(); } } return 0; } void runAdminSession(std::vector<User>& users) { std::vector<Book> libraryBooks = loadBooks(); bool booksAreSorted = false; while(true) { std::cout << "Admin Menu:\n"; std::cout << "1. Add Book\n"; std::cout << "2. Remove Book\n"; std::cout << "3. Check Out Book\n"; std::cout << "4. Return Book\n"; std::cout << "5. Sort Books by Title\n"; std::cout << "6. Show All Books\n"; std::cout << "7. Show Promotional Books\n"; std::cout << "8. Exit\n"; std::cout << "Choose an option: "; int choice; std::cin >> choice; if (std::cin.fail()) { std::cin.clear(); std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n'); std::cout << "Invalid input. Please enter a number." << std::endl; continue; } if (choice < 1 || choice > 8) { std::cout << "Invalid choice. Please try again." << std::endl; continue; } switch (choice) { case 1: addBook(libraryBooks); booksAreSorted = false; break; case 3: checkOutBook(libraryBooks); break; case 4: returnBook(libraryBooks); break; case 5: sortBooksByTitle(libraryBooks); booksAreSorted = true; break; case 7: showPromotionalBooks(); break; case 8: return; default: std::cout << "Invalid choice. Please try again." << std::endl; break; } saveBooks(libraryBooks); saveUsers(users); showPromotionalBooks(); std::cout << "--- Updated Book List ---" << std::endl; for (const auto& book : libraryBooks) { std::cout << "ID: " << book.id << ", Title: " << book.title << std::endl; } } } void runUserSession() { }

---6b3876caed0d4d8b\models\Book.h---
using namespace std; struct Book { int id; string title; string author; bool isAvailable; };

---6b3876caed0d4d8b\models\User.h---
struct User { std::string username; std::string password; };

---6b3876caed0d4d8b\utils\LibraryUtils.cpp---
const std::string BOOKS_FILENAME = "data/books.dat"; const std::string USERS_FILENAME = "data/users.dat"; const std::string ADMIN_USERNAME = "admin"; const std::string ADMIN_PASSWORD = "admin123"; Role loginUser(const std::vector<User>& users) { } void registerUser(std::vector<User>& users) { } std::vector<User> loadUsers() { } void saveUsers(const std::vector<User>& users) { } void saveBooks(const std::vector<Book>& books) { std::ofstream file(BOOKS_FILENAME); if (!file.is_open()) return; for (const auto& book : books) { file << book.id << "|" << book.title << "|" << book.author << "|" << book.isAvailable << std::endl; } } std::vector<Book> loadBooks() { std::vector<Book> books; std::ifstream file(BOOKS_FILENAME); if (!file.is_open()) return books; Book b; std::string line; while (std::getline(file, line)) { size_t p1 = line.find('|'); size_t p2 = line.find('|', p1 + 1); size_t p3 = line.find('|', p2 + 1); b.id = std::stoi(line.substr(0, p1)); b.title = line.substr(p1 + 1, p2 - p1 - 1); b.author = line.substr(p2 + 1, p3 - p2 - 1); b.isAvailable = (line.substr(p3 + 1) == "1"); books.push_back(b); } return books; } void addBook(std::vector<Book>& books) { Book newBook; newBook.id=books.size()+1; std::cout<<"Enter title:";std::cin>>newBook.title; std::cout<<"Enter author:";std:cin>>newBook.author; newBook.isAvailable=true; books.push_back(newBook); std::cout<<"Book"<<newBook.title<<"by "<<newBook.author<<"ID "<<newBook.id<<"succesfully"<<newBook.isAvailable; } void displayAllBooks(const std::vector<Book>& books) { std::cout << "ID\tTitle\tAuthor\tAvailable\n"; for (const auto& book : books) { std::cout << book.id << "\t" << book.title << "\t" << book.author << "\t" << (book.isAvailable ? "Yes" : "No") << std::endl; } } Book* findBookById(std::vector<Book>& books, int id) { for(auto& book : books){ if(book.id=id){ return &book; } } return nullptr; } void checkOutBook(std::vector<Book>& books) { int bookId; std::cout<<"ID of the book to check out:"; Book* bookptr=findBookById(books,bookId); if(bookptr != nullptr){ if(bookptr->isAvailable){ bookptr->isAvailable=false; }else std::cout<<"Error the book is already check out "<<std::endl; }else std::cout<<"Error the book not found"; } void returnBook(std::vector<Book>& books) { std::cout<<"ID of the book to return:"; int bookId; Book* bookptr = findBookById(books, bookId); if (bookptr == nullptr) { std::cout << "Error: Book not found." << std::endl; return; } if (bookptr->isAvailable) { std::cout << "Error: Book is already available." << std::endl; return; } bookptr->isAvailable = true; std::cout << "Book '" << bookptr->title << "' returned successfully." << std::endl; std::cout << "ID: " << bookptr->id << ", Author: " << bookptr->author << ", Availability: " << (bookptr->isAvailable ? "Available" : "Checked Out") << std::endl; } void sortBooksByTitle(std::vector<Book>& books) { int n = books.size(); for (int i = 0; i < n - 1; ++i) { for (int j = 0; j < n - i - 1; ++j) { if (books[j].title > books[j + 1].title) { std::swap(books[j], books[j + 1]); } } } } void binarySearchByTitle(const std::vector<Book>& books) { std::string searchTitle; std::cout << "Title to find: "; std::cin >> searchTitle; int low = 0; int high = books.size() - 1; while (low <= high) { int mid = low + (high - low) / 2; if (books[mid].title == searchTitle) { std::cout << "Book found: ID: " << books[mid].id << ", Title: " << books[mid].title; } } }

---6b3876caed0d4d8b\utils\LibraryUtils.h---
enum class Role { NONE, ADMIN, USER }; void saveUsers(const std::vector<User>& users); std::vector<User> loadUsers(); void registerUser(std::vector<User>& users); Role loginUser(const std::vector<User>& users); void saveBooks(const std::vector<Book>& books); std::vector<Book> loadBooks(); void addBook(std::vector<Book>& books); void displayAllBooks(const std::vector<Book>& books); Book* findBookById(std::vector<Book>& books, int id); void checkOutBook(std::vector<Book>& books); void returnBook(std::vector<Book>& books); void sortBooksByTitle(std::vector<Book>& books); void binarySearchByTitle(const std::vector<Book>& books);

