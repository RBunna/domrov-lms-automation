FROM python:3.11-alpine

# Install Git and bash (optional)
RUN apk add --no-cache git bash

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .

CMD ["python", "main.py"]
